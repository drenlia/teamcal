# Host port exposed by Docker (also used inside the container)
PORT=3004

# Set to true to show the hourly demo reset countdown in the UI
DEMO_MODE=false

# Development:  docker compose -f docker-compose.dev.yml up --build
# Production:   docker compose up --build -d
